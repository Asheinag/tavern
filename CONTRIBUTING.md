# Как внести вклад

Спасибо за интерес к проекту! Это веб-приложение для проведения настольных ролевых игр «для себя и друзей». Прежде чем начинать — пара правил, они строгие.

## Рабочий процесс (обязательно)
- **Форк → ветка → Pull Request.** Прямые коммиты в `main` запрещены.
- **Минимальные фичи.** Одна задача = один небольшой законченный PR. Большие изменения разбивай.
- **Имена веток:** `feat/...`, `fix/...`, `chore/...`.
- **Ветка — от свежего `main`.**
- **Мерж — только после ревью и аппрува владельца.** Никто не мержит свой PR сам.
- `main` всегда остаётся собираемым и запускаемым.

## Стек и команды
- Backend: Python + FastAPI, пакеты через **uv**.
- Frontend: Vue 3 + TypeScript + Pinia, **Vite + npm**.
- БД: PostgreSQL 16, миграции — **Alembic**.

```bash
docker compose up                  # поднять БД + бэк + фронт
cd backend && uv run uvicorn app.main:app --reload
cd backend && uv run alembic upgrade head
cd frontend && npm run dev
```

Перед PR прогони линт и тесты (когда будут настроены: ruff + pytest на бэке, eslint + vitest на фронте).

## Что НЕ принимается без обсуждения
- Смена стека или ключевых архитектурных решений.
- Тяжёлые зависимости ради мелкой задачи.
- Изменения дизайна в обход эталона-прототипа.
- Реализация аутентификации, пока вопрос открыт (см. архитектуру §6).
- Фичи, выходящие за рамки текущей вехи (архитектура §9).

## Issues
Нашёл баг или есть идея — заведи Issue по шаблону (🐞 Баг / ✨ Фича). Общие вопросы — в Discussions.

## Соглашения по коду
- Бэк: Pydantic-схемы (DTO) отдельно от ORM-моделей; роутеры тонкие, логика — в сервисах/менеджерах.
- Фронт: состояние — в Pinia-сторах (`campaign` / `live` / `player`).
- Имена сущностей — как в архитектуре (`games`, `scenes`, `edges`, `artifacts`, `game_players`, `session_log`).
- Комментарии и UI-тексты — на русском.

Спасибо! 🎲
